# HGRP – Hand Gesture Recognition Project

A simple Python project that tracks and recognizes hand gestures using computer vision.

## ✅ Overview
- Detects hand landmarks and gestures via camera.  
- Built with `HandTrackingModule.py` and `HandTrackingMin.py`.  
- Ideal for live gesture-control prototypes, UI interaction, or learning computer vision basics.

## 🧑‍💻 Getting Started
```bash
git clone https://github.com/Ialbashair/HGRP.git
cd HGRP
pip install -r requirements.txt     # (or `pip install opencv-python mediapipe`)
python HandTrackingMin.py            # or HandTrackingModule.py
